const state = {
  items: []
}

// getters
const getters = {

}

// actions
const actions = {

}

// mutations
const mutations = {
  delete: state => state.items.splice(index, 1)
}

export default {
  state,
  getters,
  actions,
  mutations
}