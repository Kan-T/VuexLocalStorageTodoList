const state = {
  showPop:false,
}

// getters
const getters = {
}

// actions
const actions = {
}

// mutations
const mutations = {
  setPop(state,val){
    state.showPop = val
  },
}

export default {
  state,
  getters,
  actions,
  mutations
}